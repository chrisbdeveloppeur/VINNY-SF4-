{# Affichage des messages flash #}
{% for type, message_list in app.flashes %}
    {% for message in message_list %}
        <div class="notification is-{{ type }} is-flex justify-content-center align-items-center column has-text-weight-bold is-size-3-touch" style="color: black">
            <button class="delete" onclick="this.parentElement.parentElement.removeChild(this.parentElement);"></button>
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}