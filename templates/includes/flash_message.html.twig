{# Affichage des messages flash #}
{% for type, message_list in app.flashes %}
    {% for message in message_list %}
{#        <div class="notification is-{{ type }} is-flex justify-content-center align-items-center column">#}
{#            <button class="delete" onclick="this.parentElement.parentElement.removeChild(this.parentElement);"></button>#}
{#            #}
{#        </div>#}

        <article class="message is-small is-success is-size-6-desktop">
            <div class="message-header">
                <div class="is-size-5 is-size-6-touch has-text-weight-bold">{% trans %}Operation success !{% endtrans %}</div>
                <span class="icon is-small has-text-white">
                    <i class="fas fa-lg fa-check"></i>
                </span>
            </div>
            <div class="message-body">
                {{ message }}
            </div>
        </article>
    {% endfor %}
{% endfor %}